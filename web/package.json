{
  "name": "inphase-web",
  "version": "0.1.1",
  "private": true,
  "license": "GPL-3.0-or-later",
  "description": "InPhase browser player + host dashboard. Vanilla TypeScript, no runtime framework (architecture report §17).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "node --test --import tsx src/",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "lucide": "^1.41.0"
  }
}
